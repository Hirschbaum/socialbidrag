import React from "react";
import { Container, Box, Typography } from "@material-ui/core";

const Header = () => {
  return (
    <Container>
      <Box></Box>
      <Box>
        <Typography variant="h4">HELSINGBORG</Typography>
      </Box>
    </Container>
  );
};

export default Header;
